import { useContext } from "react";
import { searchResultsCtx } from "../context/Products";

function SearchResults() {

    const searchResults = useContext(searchResultsCtx);


}

export default SearchResults;